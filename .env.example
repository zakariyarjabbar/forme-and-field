# Local file; production requires a persistent volume. No external credentials needed.
DATABASE_PATH=./data/forme-field.sqlite
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# Set COOKIE_SECURE=true behind HTTPS. Local HTTP must leave this false.
COOKIE_SECURE=false
